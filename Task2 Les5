using MyFourProjekt;
using System;
using System.Linq;
using System.Text;

class Task2
{
    private View view = new View();
    public void Main()
    {
        view.Print("Исходный текст.");
        view.Print(String.Join(" ", "Пробую написать программу, я новичок в программировании и не всё еще получается, но я очень стараюсь."));

        view.Print("а) Вывести только те слова сообщения, которые содержат не более 8 букв.");
        view.Print(String.Join(" ", "Пробую написать программу, я новичок в программировании и не всё еще получается, но я очень стараюсь.".Split().Where(x => x.Length < 8)));

        view.Print("б) Удалить из сообщения все слова, которые заканчиваются на символ Е.");
        view.Print(String.Join(" ", "Пробую написать программу, я новичок в программировании и не всё еще получается, но я очень стараюсь.".Split().Where(x => x[x.Length - 1] != 'е')));

        view.Print("в) Найти самое длинное слово сообщения.");
        view.Print(String.Join(" ", "Пробую написать программу, я новичок в программировании и не всё еще получается, но я очень стараюсь.".Split(' ').OrderBy(m => m.Length).Last()));
        view.Pause();
    }
}